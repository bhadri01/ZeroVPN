global:
  scrape_interval: 15s
  evaluation_interval: 30s

scrape_configs:
  - job_name: zerovpn-api
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8080"]
        labels:
          instance: zerovpn-api
